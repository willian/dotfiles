# World Clock

Build the debug companion app with `pnpm tauri build --debug` from this directory.

SketchyBar opens it through `./toggle`. The bundled debug app is used first; the release bundle is used when present instead.

Run `pnpm check` and `pnpm test:time` before rebuilding after changes.
